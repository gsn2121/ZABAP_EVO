projection;
strict ( 2 );
use draft;

define behavior for ZC_GSN_RAP_TRAVEL alias Travel
use etag

{
  use create;
  use update;
  use delete;
  use action SetApprove;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}